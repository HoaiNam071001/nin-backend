export enum Role {
  STUDENT = 'student',
  TEACHER = 'teacher',
  EDUCATION_MANAGER = 'education-manager',
  ADMIN = 'admin',
}

export enum CourseStatus {
  DRAFT = 'draft',
  PENDING = 'pending',
  READY = 'ready',
  CLOSED = 'closed',
  DELETED = 'deleted',
}

export enum SectionType {
  'video',
  'post',
}
